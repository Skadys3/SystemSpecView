# Кроссплатформенные системные метрики (CPU, память, диски, сеть, батарея)
psutil>=5.9.0

# Привязки к Windows Management Instrumentation — используются для получения
# сведений о материнской плате, BIOS и видеокарте. Устанавливаются только на
# Windows; приложение корректно деградирует (поля показывают "Н/Д"), если
# этих пакетов нет в другой ОС.
wmi>=1.5.1; platform_system == "Windows"
pywin32>=306; platform_system == "Windows"

# NVIDIA Management Library — используется для получения актуальных данных
# об использовании видеопамяти GPU в реальном времени (обходит ограничение
# WMI на переполнение 32-битного поля AdapterRAM). Работает только с
# видеокартами NVIDIA; на других GPU приложение просто не получит "живые"
# данные об использованной видеопамяти.
pynvml>=11.5.0; platform_system == "Windows"